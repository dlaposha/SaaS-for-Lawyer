name: Tests

on: [push, pull_request]

jobs:
  test-suite:
    name: Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Run tests
        run: |
          echo "Running test suite..."
          exit 0